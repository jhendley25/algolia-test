// ok, this might be a bad idea
// but I like the idea of a general manifest for views

// this could potentially be applied to all scripts.
// have a top-level manifest.js file, which all fiels inherit from
// makes things cleaner (maybe?)

import first from "./first"
import second from "./second"

export {first, second};
